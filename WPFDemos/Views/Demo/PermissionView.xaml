<Window x:Class="WPFDemos.Views.Demo.PermissionView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFDemos.Views.Demo"
        mc:Ignorable="d"
        Title="PermissionView" Height="450" Width="800">

    <Window.DataContext>
        <Binding Source="{StaticResource Locator}" Path="PermissionView"></Binding>
    </Window.DataContext>

    <Grid>
        <StackPanel>
            <Button Height="50" Width="100" Content="view" Command="{Binding ViewCommand}"/>
        </StackPanel>
    </Grid>
</Window>
